import { EmptyspaceDirective } from './emptyspace.directive';

describe('EmptyspaceDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyspaceDirective();
    expect(directive).toBeTruthy();
  });
});
