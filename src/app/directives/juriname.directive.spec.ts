import { JurinameDirective } from './juriname.directive';

describe('JurinameDirective', () => {
  it('should create an instance', () => {
    const directive = new JurinameDirective();
    expect(directive).toBeTruthy();
  });
});
